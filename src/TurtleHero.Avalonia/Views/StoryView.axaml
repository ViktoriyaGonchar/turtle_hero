<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="TurtleHero.Avalonia.Views.StoryView">
    <UserControl.Styles>
        <Style Selector="TextBlock.story-text">
            <Setter Property="FontFamily" Value="Georgia, serif"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="LineHeight" Value="1.8"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="#D4AF37"/>
        </Style>
    </UserControl.Styles>
    
    <Border Background="#2A1810" Padding="40">
        <Border.Styles>
            <Style Selector="Border">
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.5"/>
                    </Setter.Value>
                </Setter>
            </Style>
        </Border.Styles>
        
        <ScrollViewer>
            <StackPanel Spacing="20">
                <TextBlock Text="📜 Легенда Эмодзи-Архипелага 📜" 
                          FontSize="32" 
                          HorizontalAlignment="Center"
                          Foreground="#D4AF37"
                          Margin="0,0,0,20"/>
                
                <TextBlock x:Name="StoryTextBlock" 
                          Classes="story-text"
                          Text="Загрузка истории..."/>
                
                <Button x:Name="ContinueButton" 
                       Content="Продолжить →" 
                       HorizontalAlignment="Center"
                       Margin="0,30,0,0"
                       Padding="20,10"
                       FontSize="16"
                       Click="ContinueButton_Click"/>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>

